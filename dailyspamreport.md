# Proxmox Mail Gateway：Daily Spam Report（日次スパムレポート）利用ガイド

Daily Spam Reportは、前日にスパム（迷惑メール）として隔離されたメールがある場合、**毎日午前0時過ぎに各受信者宛てに自動送信される通知メール**です。隔離されたメールがない日は送信されません。

このレポートを利用して、隔離されたメールの確認や受信許可などの操作を行うことができます。

---

## 1. メール本文のリンクから管理画面で操作する

レポートに記載されている**メールのタイトル（件名）をクリック**すると、専用の「スパムメール管理ページ」がブラウザで開きます。
該当のメールが選択された状態で表示されますが、他にも隔離されたメールがある場合は、左側のペイン（一覧）から続けて内容を確認できます。

画面左上のボタンから、以下の操作が可能です。

* **Welcomelist（ホワイトリスト）**
  * 送信者を安全な送信者として登録します。以後、この相手からのメールは隔離されにくくなります。
* **Deliver（配送）**
  * 隔離されたメールを通常の受信箱へ配送します。クリック後、普段お使いのメーラーで受信できるようになります。
* **ダウンロード**
  * メールを `.eml` 形式でローカルPCに保存します。
* **Blocklist（ブロックリスト）**
  * 送信者を拒否リストに登録します。※現在の巧妙なスパムに対してはあまり効果がないため、基本的には使用しなくてかまいません。
* **Delete（削除）**
  * メールを削除します。※隔離されたメールは一定期間が経つと**自動的に消去されるため、都度手動で削除する必要はありません。**

> 💡 **アドレスの直接登録**
> 画面の最左ペインにある「Welcomelist」メニューからは、特定のメールアドレスを手動で直接ホワイトリストに追加・管理することも可能です。

---

## 2. レポートメールから直接操作する（クイックアクション）

Daily Spam Reportのメール一覧の右側には、管理画面を開かずにその場で処理できるクイックリンクが用意されています。

* **Welcomelist** ： 管理画面の「Welcomelist」と同じ（推奨）
* **Deliver** ： 管理画面の「Deliver」と同じ（通常の受信箱に届きます）
* **Blocklist** ： 管理画面の「Blocklist」と同じ（効果が薄いため非推奨）
* **Delete** ： 管理画面の「Delete」と同じ（自動消去を待ってもOKです）

---

## 3. レポート（メール）が手元にない状態から管理画面に入る方法

定期的な確認などで、Daily Spam Reportのメールを経由せずに管理画面へログインしたい場合は、以下の手順で行えます。

1. ブラウザで指定のURLにアクセスします。
2. 画面内にある **[Quarantine リンクの要求]** ボタンをクリックします。
3. ご自身のメールアドレスを入力して送信します。
4. 入力したアドレス宛に「ログイン用リンク」が記載されたメールが届くので、URLをクリックして管理画面にアクセスしてください。
